VITE_PAYMENT_API_BASE_URL=http://localhost:4000/api

# MSG91 OTP Widget ID — safe client-side on its own. tokenAuth is NOT a
# frontend variable; it's backend-only (see backend/.env.example) and
# fetched by the frontend at runtime via /api/auth/widget/config.
VITE_MSG91_WIDGET_ID=
